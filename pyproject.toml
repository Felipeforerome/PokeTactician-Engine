[project]
name = "poketactician-engine"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = [
    "ipykernel>=6.10.0",
    "pandas>=2.3.1",
    "pymoo>=0.6.1.5",
]
[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "pytest-cov>=7.0.0",
]
[tool.coverage.run]
omit = ["poketactician_old/*"]
